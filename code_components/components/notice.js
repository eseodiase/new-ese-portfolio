const notice =()=>{
    return `<section>
    <div class="notice">
  <h3>Displaying Bitcoin Symbol (&#8383), Naira Symbol (&#8358) and Other Characters and Icons in HTML Needs to Start with:
  <span class="reduce_font">
    ampersand followed by hash symbol (&#).
    </span>
    </h3>
    </div>
    </section>`;
}
export default notice;